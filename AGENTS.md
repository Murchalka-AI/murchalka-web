# Contributor guidance

- Keep the shell product-agnostic and render only universal Client Runtime components.
- Keep React components in strict TypeScript and preserve production type checking.
- Never use innerHTML for module or model content.
- Preserve keyboard access, focus visibility, live regions, reduced motion, and semantic labels.
- Connect only to an explicit loopback realtime endpoint in the Phase 5 profile.
